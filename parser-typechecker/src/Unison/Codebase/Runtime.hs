module Unison.Codebase.Runtime where

-- import qualified Unison.UnisonFile as UF

-- type UnisonFile v = UF.UnisonFile v Ann

-- data Runtime v = Runtime { launch :: IO (IO (), Term v -> Codebase v -> IO ()) }

{-

foo x =
  x + 1
  x

> foo 3

-}
