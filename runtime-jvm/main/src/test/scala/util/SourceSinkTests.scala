package org.unisonweb.util

import org.unisonweb.EasyTest._

object SourceSinkTests {
  val tests =
    test("source/sink") { implicit T =>
      fail("this test needs writing")
      //val input = replicate(20)
      //val bytes = Sink.toChunks(10) { sink =>
      //  replicate(20)
      //}
      ok
    }

  // def source: Source =
}

