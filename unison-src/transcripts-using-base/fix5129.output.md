``` ucm :hide
> builtins.mergeio lib.builtins
```

Checks for some bad type checking behavior. Some ability subtyping was
too lenient when higher-order functions were involved.

``` unison :error
foreach : (a ->{g} ()) -> [a] ->{g} ()
foreach f = cases
  [] -> ()
  x +: xs ->
    f x
    foreach f xs

forkIt : '{IO} () ->{IO} ()
forkIt e =
  _ = IO.forkComp e
  ()

thunk : '{IO,Exception} ()
thunk = do
  raise (Failure (typeLink MiscFailure) "thunk" (Any ()))

go = do
  foreach forkIt [thunk]
```

``` ucm :added-by-ucm
  Loading changes detected in scratch.u.

  I found an ability mismatch when checking the expression in red
      8 | forkIt : '{IO} () ->{IO} ()
      9 | forkIt e =
     10 |   _ = IO.forkComp e
     11 |   ()
     12 | 
     13 | thunk : '{IO,Exception} ()
     14 | thunk = do
     15 |   raise (Failure (typeLink MiscFailure) "thunk" (Any ()))
     16 | 
     17 | go = do
     18 |   foreach forkIt [thunk]

  The check that

      Unit ->{IO, Exception} Unit

  is a subtype of

      Unit ->{IO} Unit

  failed because

      {Exception}

  is not a subtype of

      {IO}

```

This comes from issue \#3513

``` unison :error
(<<) : (b ->{e} c) -> (a ->{e} b) -> a ->{e} c
(<<) f g x = f (g x)

catchAll.impl : '{IO, Exception} a ->{IO} Either Failure a
catchAll.impl thunk =
  handle tryEval do catch thunk
  with
    cases
      { x }                    -> x
      {Exception.raise f -> _} -> Left f

fancyTryEval : '{g, IO, Exception} a ->{g, IO, Exception} a
fancyTryEval = reraise << catchAll.impl
```

``` ucm :added-by-ucm
  Loading changes detected in scratch.u.

  I found an ability mismatch when checking the expression in red
      1 | (<<) : (b ->{e} c) -> (a ->{e} b) -> a ->{e} c
      .
     12 | fancyTryEval : '{g, IO, Exception} a ->{g, IO, Exception} a
     13 | fancyTryEval = reraise << catchAll.impl

  The check that

      (Unit ->{IO, Exception} 𝕩84) ->{𝕖78, Exception, 𝕖85, IO} 𝕩84

  is a subtype of

      (Unit ->{g69, IO, Exception} a66) ->{g69, IO, Exception} a66

  failed because

      {g69}

  is not a subtype of

      {IO, Exception}

```
