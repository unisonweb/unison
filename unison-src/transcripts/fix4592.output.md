```unison
doc = {{ {{ bug "bug"
  52 }} }}
```

```ucm

  Loading changes detected in scratch.u.

  I found a closing '}}' here without a matching 'syntax.docTransclude'.
  
      2 |   52 }} }}
  

```
