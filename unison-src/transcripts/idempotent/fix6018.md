Checks that pattern matching against a universal type inside a data type
works. This was intended, but was stymied by backwards subtyping checks in
the pattern checker.

``` unison
type T r = T (forall x. (x -> r, x))

runT = cases
  T (f, x) -> f x
```

``` ucm :added-by-ucm
  Loading changes detected in scratch.u.

  + type T r

  + runT : T r -> r

  Run `update` to apply these changes to your codebase.
```
