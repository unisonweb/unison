``` ucm :hide
> builtins.merge lib.builtin
```

``` unison
direct.foo = 17
direct.lib.indirect.foo = 18

thing = indirect.foo + indirect.foo
```

``` ucm :added-by-ucm
  Loading changes detected in scratch.u.

  + direct.foo              : Nat
  + direct.lib.indirect.foo : Nat
  + thing                   : Nat

  Run `update` to apply these changes to your codebase.
```

``` ucm
> update

  Okay, I'm searching the branch for code that needs to be
  updated...

  Done.

> move direct.foo lib.direct.foo

  Done.

> move direct.lib.indirect.foo lib.direct.lib.indirect.foo

  Done.

> view thing

  thing : Nat
  thing =
    use Nat +
    use indirect foo
    foo + foo

> edit.new thing
```

``` unison :added-by-ucm scratch.u
thing : Nat
thing =
  use Nat +
  use indirect foo
  foo + foo
```
