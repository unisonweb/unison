``` ucm :hide
> builtins.merge
```

``` unison
foo =
  _ = 1
  _ = 22
  42

> foo + 20
```

``` ucm :added-by-ucm
  Loading changes detected in scratch.u.

  + foo : Nat

  Run `update` to apply these changes to your codebase.

    6 | > foo + 20
          ⧩
          62
```
