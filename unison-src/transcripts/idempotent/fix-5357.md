``` unison
util.ignore : a -> ()
util.ignore _ = ()

foo : ()
foo =
  ignore 3
  ignore 4
```

``` ucm :added-by-ucm
  Loading changes detected in scratch.u.

  + foo         : ()
  + util.ignore : a -> ()

  Run `update` to apply these changes to your codebase.
```

``` ucm
> add

  Okay, I'm searching the branch for code that needs to be
  updated...

  Done.
```

``` unison
base.ignore : a -> ()
base.ignore _ = ()
```

``` ucm :added-by-ucm
  Loading changes detected in scratch.u.

  + base.ignore : a -> ()
      (also named util.ignore)

  Run `update` to apply these changes to your codebase.
```

``` ucm
> update

  Okay, I'm searching the branch for code that needs to be
  updated...

  Done.

> move base.ignore lib.base.ignore

  Done.

> edit.namespace .

> load

  Loading changes detected in scratch.u.

  No changes found.
```

``` unison :added-by-ucm scratch.u
foo : ()
foo =
  use util ignore
  ignore 3
  ignore 4

util.ignore : a -> ()
util.ignore _ = ()
```
