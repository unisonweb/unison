``` ucm :hide
> builtins.merge lib.builtin
```

``` unison
direct.foo = 17
direct.lib.indirect.foo = 18

bar : Nat
bar = direct.foo + direct.foo
```

``` ucm :added-by-ucm
  Loading changes detected in scratch.u.

  + bar                     : Nat
  + direct.foo              : Nat
  + direct.lib.indirect.foo : Nat

  Run `update` to apply these changes to your codebase.
```

Here, `bar` renders as `foo + foo`, even though there are two names with suffix `foo` in scope, because one is an
indirect dependency. It used to render as `direct.foo + direct.foo`.

``` ucm
> update

  Okay, I'm searching the branch for code that needs to be
  updated...

  Done.

> move direct.foo lib.direct.foo

  Done.

> move direct.lib.indirect.foo lib.direct.lib.indirect.foo

  Done.

> view bar

  bar : Nat
  bar =
    use Nat +
    foo + foo
```

Same test, but for types.

``` unison
type direct.Foo = MkFoo
type direct.lib.indirect.Foo = MkFoo

type Bar = MkBar direct.Foo
```

``` ucm :added-by-ucm
  Loading changes detected in scratch.u.

  + type Bar
  + type direct.Foo
  + type direct.lib.indirect.Foo

  Run `update` to apply these changes to your codebase.
```

``` ucm
> update

  Okay, I'm searching the branch for code that needs to be
  updated...

  Done.

> move direct.Foo lib.direct.Foo

  Done.

> move direct.lib.indirect.Foo lib.direct.lib.indirect.Foo

  Done.

> view Bar

  type Bar = MkBar Foo
```
