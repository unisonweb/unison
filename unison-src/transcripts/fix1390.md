
```ucm
.> builtins.merge
```

```unison
-- List.map : (a -> b) -> [a] -> [b]
List.map f as =
  go acc = cases
    [] -> acc
    h +: t -> go (acc :+ f h) t
  go []
```

```ucm
.> add
.> view List.map
```
