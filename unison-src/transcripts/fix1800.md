
```ucm:hide
.> builtins.mergeio
```

```ucm
.> ls builtin.io2.IO
```

```unison
printLine : Text ->{IO} ()
printLine msg =
  putBytes.impl (stdHandle StdOut) (Text.toUtf8 msg)
  ()

mymain = '(printLine "hello world!")
```

```ucm
.> run mymain
```
