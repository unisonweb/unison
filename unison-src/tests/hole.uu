-- A test tracking the fact that holes aren't supported yet.

a : ()
-- a = ()   -- works if I uncomment this line

()

-- Typechecker emits the following error:
--
--  typechecker.tests/hole.u FAILURE tests/hole.u:6:2:
--  unexpected )
--  expecting symbol
--   3 | ()
