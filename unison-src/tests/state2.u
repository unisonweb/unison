--State2 ability
ability State se2 where
  put : ∀ se . se -> {State se} ()
  get : ∀ se . () -> {State se} se
state : ∀ s a . s -> Request (State s) a -> (s, a)
state woot eff = case eff of
  { State.get () -> k } -> handle (state woot) with (k woot)
  { State.put snew -> k } -> handle (state snew) with (k ())
  { a } -> (woot, a)

> ()
